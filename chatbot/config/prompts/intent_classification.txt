Classify the user's latest message in the context of a luxury travel concierge conversation.
Return ONLY valid JSON with no additional text.

{{
  "primary_intent": "one of: general_enquiry, destination_info, pricing_question, itinerary_request, booking_intent, lodge_info, travel_logistics, complaint, review_share, callback_request, brochure_request, off_topic",
  "urgency": "one of: low, medium, high, critical",
  "urgency_reason": "string or null — explain if high/critical, e.g. 'Travel date within 7 days'",
  "booking_stage": "one of: browsing, considering, ready_to_book, booked_followup",
  "sentiment": "one of: positive, neutral, negative",
  "requires_human": "boolean — true ONLY for complaints, callback requests, legal threats, or genuinely bizarre requests",
  "escalation_reason": "string or null — explain if requires_human is true",
  "detected_keywords": "array of relevant keywords from the message"
}}

RULES FOR requires_human:
- TRUE only for: explicit callback/human requests, complaints, refund/cancellation demands, legal threats, or genuinely impossible/bizarre requests
- FALSE for: pricing questions, booking intent, itinerary requests, destination queries, availability questions, budget discussions, general travel advice, sharing contact details
- The bot handles ALL normal travel enquiries. Do NOT set requires_human just because the client wants to book or asks about pricing.

LATEST MESSAGE: {message}

RECENT CONVERSATION CONTEXT (last 3 messages):
{recent_context}
